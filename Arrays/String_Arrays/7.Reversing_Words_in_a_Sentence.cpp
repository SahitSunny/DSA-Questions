#include<iostream>
#include<string>
using namespace std;



void reversing(string s)
{
    string temp ="";

    for(int i=0; i<s.length(); i++)
    {
        char ch;
        ch[i] = s[i]
        if(ch[i] != " ")
        {
            temp.push_back(ch[i])
        }
    }
    cout<<temp;
    
int main()
{
    string str;
    cin>>str;
    reversing(str);
    return 0;
}